FROM scratch

LABEL org.opencontainers.image.title="PLwC private staging bootstrap"
LABEL org.opencontainers.image.description="Unlinked bootstrap used to establish private GHCR package visibility before runtime images are staged."
LABEL org.opencontainers.image.licenses="Apache-2.0"


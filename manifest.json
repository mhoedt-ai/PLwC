{
  "manifest_version": "0.4",
  "name": "plwc-gateway",
  "display_name": "PLwC Gateway",
  "version": "1.0.0",
  "description": "Policy-controlled MCP gateway for PLwC 1.0 with eight governed public tools, local profiles, controlled memory, document operations and sandbox policy.",
  "long_description": "PLwC Gateway 1.0 exposes one policy-controlled MCP boundary with exactly eight public facade tools, Safe Mode, Docker sandbox policy, scoped workspace access, document and archive operations, profile governance, controlled reflection and memory flows, local audit logging, and the PLwC Chat Bridge 1.0 integration. Qdrant remains optional and disabled by default. The Windows Setup and MCPB may be distributed unsigned and are therefore bound to exact SHA-256 evidence.",
  "author": {
    "name": "PLwC"
  },
  "server": {
    "type": "python",
    "entry_point": "server.py",
    "mcp_config": {
      "command": "python",
      "args": [
        "${__dirname}/server.py"
      ],
      "env": {
        "PLWC_WORKSPACE_ROOT": "${user_config.workspace_path}",
        "PLWC_PROFILE_ROOT": "${user_config.profiles_path}",
        "PLWC_ACTIVE_PROFILE_NAME": "${user_config.active_profile_name}",
        "PLWC_CONFIG_FILE": "${user_config.security_config}",
        "PLWC_MEMORY_WRITE_THRESHOLD": "${user_config.memory_write_threshold}",
        "PLWC_PERSONA_WRITE_THRESHOLD": "${user_config.persona_write_threshold}",
        "PLWC_QDRANT_ENABLED": "${user_config.qdrant_enabled}",
        "PLWC_TEMPERAMENT_WRITE_THRESHOLD": "${user_config.temperament_write_threshold}",
        "PLWC_PERSONA_LAYER_DISABLED": "${user_config.persona_layer_disabled}"
      },
      "platform_overrides": {
        "win32": {
          "command": "python",
          "args": [
            "${__dirname}/server.py"
          ],
          "env": {
            "PLWC_WORKSPACE_ROOT": "${user_config.workspace_path}",
            "PLWC_PROFILE_ROOT": "${user_config.profiles_path}",
            "PLWC_ACTIVE_PROFILE_NAME": "${user_config.active_profile_name}",
            "PLWC_CONFIG_FILE": "${user_config.security_config}",
            "PLWC_MEMORY_WRITE_THRESHOLD": "${user_config.memory_write_threshold}",
            "PLWC_PERSONA_WRITE_THRESHOLD": "${user_config.persona_write_threshold}",
            "PLWC_QDRANT_ENABLED": "${user_config.qdrant_enabled}",
            "PLWC_TEMPERAMENT_WRITE_THRESHOLD": "${user_config.temperament_write_threshold}",
            "PLWC_PERSONA_LAYER_DISABLED": "${user_config.persona_layer_disabled}"
          }
        },
        "darwin": {
          "command": "python3",
          "args": [
            "${__dirname}/server.py"
          ],
          "env": {
            "PLWC_WORKSPACE_ROOT": "${user_config.workspace_path}",
            "PLWC_PROFILE_ROOT": "${user_config.profiles_path}",
            "PLWC_ACTIVE_PROFILE_NAME": "${user_config.active_profile_name}",
            "PLWC_CONFIG_FILE": "${user_config.security_config}",
            "PLWC_MEMORY_WRITE_THRESHOLD": "${user_config.memory_write_threshold}",
            "PLWC_PERSONA_WRITE_THRESHOLD": "${user_config.persona_write_threshold}",
            "PLWC_QDRANT_ENABLED": "${user_config.qdrant_enabled}",
            "PLWC_TEMPERAMENT_WRITE_THRESHOLD": "${user_config.temperament_write_threshold}",
            "PLWC_PERSONA_LAYER_DISABLED": "${user_config.persona_layer_disabled}"
          }
        },
        "linux": {
          "command": "python3",
          "args": [
            "${__dirname}/server.py"
          ],
          "env": {
            "PLWC_WORKSPACE_ROOT": "${user_config.workspace_path}",
            "PLWC_PROFILE_ROOT": "${user_config.profiles_path}",
            "PLWC_ACTIVE_PROFILE_NAME": "${user_config.active_profile_name}",
            "PLWC_CONFIG_FILE": "${user_config.security_config}",
            "PLWC_MEMORY_WRITE_THRESHOLD": "${user_config.memory_write_threshold}",
            "PLWC_PERSONA_WRITE_THRESHOLD": "${user_config.persona_write_threshold}",
            "PLWC_QDRANT_ENABLED": "${user_config.qdrant_enabled}",
            "PLWC_TEMPERAMENT_WRITE_THRESHOLD": "${user_config.temperament_write_threshold}",
            "PLWC_PERSONA_LAYER_DISABLED": "${user_config.persona_layer_disabled}"
          }
        }
      }
    }
  },
  "tools": [
    {
      "name": "plwc_status",
      "description": "Facade for runtime, sandbox, first-run and Claude configuration status via scope=runtime|sandbox|first_run|config."
    },
    {
      "name": "plwc_describe",
      "description": "Return read-only PLwC facade schemas, allowed dispatch scopes and operations, required fields, and common denial reasons."
    },
    {
      "name": "plwc_profile",
      "description": "Facade for governed profile status, snapshot, retrieval, Tagebuch scan and compile operations; compile defaults to compile_mode=boot, supports working with task_context and optional fresh Qdrant semantic memory, and full for audit/diagnostics."
    },
    {
      "name": "plwc_reflection",
      "description": "Facade for governed reflection writes via operation=write with English-first marker/trust aliases, target, candidate_for, evidence and entry_date validation; stored values remain canonical PBA2."
    },
    {
      "name": "plwc_governor",
      "description": "Facade for governed profile planning, confirmed apply, retire/list_retirable diagnostics, and Qdrant reindex/drop_index operations."
    },
    {
      "name": "plwc_sandbox_run",
      "description": "Facade for Docker-only sandbox execution via lang=python|shell|node; no host-shell fallback. lang=node runs a workspace-relative .js script in a separate plwc-node-runner:0.1.0 image."
    },
    {
      "name": "plwc_workspace_operation",
      "description": "Run bounded governed workspace operations: list, search, read, write, file_info, batch_read, create_dir, move, rename, exact_replace, copy (byte-exact file copy), read_binary (returns base64) and write_binary (accepts base64). write with source_path is rejected; use copy."
    },
    {
      "name": "plwc_document_operation",
      "description": "Create DOCX/XLSX/PPTX/PDF, run DOCX and XLSX Creation V2, read workspace raster images as MCP image content, run PDF MVP and PDF text-layer operations, inspect/extract/create ZIP files, and inspect/extract Office/OpenDocument files through the governed Document Worker; no conversion, non-ZIP archives, encrypted ZIPs, delete, OCR, macro execution, formula execution, signing, redaction, or form filling."
    }
  ],
  "keywords": [
    "mcp",
    "policy",
    "safe-mode",
    "docker",
    "governance"
  ],
  "license": "Apache-2.0",
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "python": ">=3.11"
    }
  },
  "user_config": {
    "workspace_path": {
      "type": "directory",
      "title": "PLwC workspace path",
      "description": "Optional workspace directory. Leave empty to use a safe user-scoped PLwC workspace.",
      "required": false
    },
    "profiles_path": {
      "type": "directory",
      "title": "PLwC profiles path",
      "description": "Optional profiles directory. Leave empty to use a safe user-scoped PLwC profiles directory.",
      "required": false
    },
    "active_profile_name": {
      "type": "string",
      "title": "Active PLwC profile",
      "description": "Name of the active PLwC profile inside the profiles path. Default: default.",
      "required": false,
      "default": "default"
    },
    "security_config": {
      "type": "file",
      "title": "PLwC security config",
      "description": "Optional local security.yaml path. Leave empty to use conservative bundled defaults and user-scoped paths.",
      "required": false
    },
    "memory_write_threshold": {
      "type": "number",
      "title": "Memory write threshold",
      "description": "Minimum confirmed evidence count before memory writes are proposed or applied. Default: 2.",
      "required": false,
      "default": 2
    },
    "persona_write_threshold": {
      "type": "number",
      "title": "Persona write threshold",
      "description": "Minimum confirmed evidence count before persona writes are proposed or applied. Default: 3.",
      "required": false,
      "default": 3
    },
    "temperament_write_threshold": {
      "type": "number",
      "title": "Temperament write threshold",
      "description": "Minimum confirmed evidence count before temperament writes are proposed or applied. Default: 2.",
      "required": false,
      "default": 2
    },
    "qdrant_enabled": {
      "type": "boolean",
      "title": "Qdrant retrieval index",
      "description": "Enable the optional Qdrant semantic retrieval index (off by default). The config-window value is authoritative.",
      "default": false
    },
    "persona_layer_disabled": {
      "type": "boolean",
      "title": "Disable persona layer in compile output",
      "description": "Turn on to omit the PERSONA block from profile compile output by default. Use for Odysseus or other explicit context-injection PoCs; hard gates and governance remain active.",
      "required": false,
      "default": false
    }
  },
  "icon": "plwc-icon-512.png"
}
